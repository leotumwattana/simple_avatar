<h1>User Index</h1>

<%= link_to "Create User", users_new_path %>

<ul class="users">
  <% @users.each do |user| %>
    <li class="user">
      <%= link_to user.email, "/users/#{user.id}" %>
    </li>
  <% end %>
</ul>
